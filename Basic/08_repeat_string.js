function repeatStringNumTimes(str, num) {
  if (num > 0) {
    return str.repeat(num)
  } else {
    return ''
  }
}

/*
function repeatStringNumTimes(str, num) {
return num > 0 ? str.repeat(num) : ""
}
*/


console.log(("abc", 3))
